const hre = require("hardhat");

async function main() {
  try {
    //}
    
  } catch (error) {
    //console.error("❌ Connection failed:", error.message);
  }
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    //console.error(error);
    process.exit(1);
  }); 