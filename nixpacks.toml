[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = [
    'npm ci --prefer-offline --no-audit --no-fund --omit=dev'
]

[phases.build]
cmds = [
    'npm run build'
]

[start]
cmd = 'npm run start:prod'

[variables]
NODE_ENV = 'production' 