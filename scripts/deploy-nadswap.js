const { ethers } = require("hardhat");
const fs = require("fs");

async function main() {
  //);
  //// Verify contract (if supported)
  //.then(() => process.exit(0))
  .catch((error) => {
    //console.error("Deployment failed:", error);
    process.exit(1);
  }); 